steps:
#- script: printenv
- script: echo "$(Build.SourcesDirectory)"
- script: echo "$(Pipeline.Workspace)"
- script: echo "$(HOME)"

- task: CacheBeta@1
  inputs:
    path: $(Pipeline.Workspace)\test
    key: \"test4223222332\" | $(Agent.OS) | $(Build.SourcesDirectory)\*.*