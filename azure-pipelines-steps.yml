steps:
#- script: printenv
- script: echo "$(Build.SourcesDirectory)"
- script: echo "$(Pipeline.Workspace)"
- script: echo "$(HOME)"

- task: CacheBeta@1
  inputs:
    path: $(Build.SourcesDirectory)
    key: \"test422322\" | $(Agent.OS) | $(Build.SourcesDirectory)\*.*